[project]
name = "hplc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eric Gemmell", email = "ericgemmell@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.10,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "numpy>=2.4.3",
    "olefile>=0.47",
    "plotly>=6.6.0",
    "pytest>=9.0.2",
]
